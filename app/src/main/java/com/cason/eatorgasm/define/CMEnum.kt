package com.cason.eatorgasm.define

class CMEnum {
    enum class EatCommand {
        CLICKED,
        LONG_CLICKED,
        BUTTON_CLICKED,
        CHECKED,
        MOVE,
        COPY,
        FAVORITE,
        RENAME,
        SEND,
        INFO,
        DELETE,
        UN_SELECT,
        IMAGE_CLICKED,

        SET_RESULT,
    }

    enum class CommonProgressType {
        UPLOAD, ETC
    }
}